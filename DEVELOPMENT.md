# 妙贴（MeldTip）· 开发说明 v1.0

> 给 KIMI 的项目全面介绍 + 两阶段开发目标

---

## 一、项目起源

**贴词吧（tieciba.com）** 是超哥于 2026 年 5 月搭建的一个提示词工具。核心功能很简单 —— 用户看到一张 AI 生成的效果图，页面下方有几个模块化的"卡片"（如【角度】【光线】【风格】），勾选不同选项后组合成一条完整的提示词，一键复制到 ComfyUI 中生成图片。

名字"贴词吧"谐音"贴糍粑"——像做糍粑一样把不同维度的提示词"贴"在一起。

**妙贴（meldtip.com）** 是贴词吧的升级版品牌。meld = 融合（有机融合为一体），妙贴 = 巧妙+秒贴+贴心+贴切四重含义。目标是把模块化提示词做成一个完整的社区和商城。

项目代码当前在 `/home/star/tieciba_repo/`，部署在 Vercel + Supabase。一套纯静态 HTML + JS + Supabase。

---

## 二、目标用户

- 室内设计师和全屋定制品牌（如溪岸、六个木匠）
- 想自己用 AI 做效果图的装修业主
- AI 图像生成的业余爱好者和内容创作者

用户不是程序员。他们要的不是"看一段代码"——是"看到效果图 → 点一下复制 → 在自己电脑上生成同样的结果"。

---

## 三、当前功能（贴词吧 v1）

| 功能 | 说明 |
|------|------|
| 模块化提示词卡片 | 用户在一页上看到多个维度的卡片（角度、光线、风格），勾选后自动拼接成一条完整 prompt |
| 一键复制 | 复制整条拼接好的提示词 |
| 方案分享 | 用户可以把当前卡片组合生成一个分享链接（`tieciba.com/s.html?id=xxx`），别人打开后看到同样的卡片和效果图 |
| 方案探索 | 一个公开页面，展示其他用户分享的方案 |
| Supabase 认证 | 邮箱注册登录 |

**当前问题：**
- 全部纯静态 HTML，每个页面是一个独立文件，没法规模化
- 提示词是硬编码在 HTML 里的，添加新提示词需要改代码
- 没有付费功能
- 没有真正的提示词数据库
- 粘贴板只支持卡片视图，不能切换到列表视图

---

## 四、商业模式（"升杯" + 佣金）

### 核心逻辑

| 层级 | 内容 | 价格 |
|------|------|------|
| 🆓 小杯（免费） | 浏览 5 条提示词，每条 1 张预览图，不能复制 | ¥0 |
| 🥤 中杯 | 全部 300+ 条提示词，多角度效果图，一键复制，列表/卡片切换 | **¥39/月** |
| 🧋 大杯 | 中杯全部 + 模块化编辑器（勾选维度改参数）+ ComfyUI 工作流 JSON 导出 | **¥99/月** |
| 🏪 创作者 | 上传自己的提示词，标价出售，平台抽佣 20% | — |

### 开源参考项目

**prompthub**（GitHub：`saifmohamedsv/prompthub`）是一个 MIT 开源的 Next.js + Supabase 提示词分享平台，包含：
- 用户注册登录（Supabase Auth：Google + GitHub OAuth）
- 创建/编辑/删除提示词，支持图片上传到 Supabase Storage
- 浏览页面 + 搜索 + 分类筛选
- 点赞收藏 + 用户主页
- shadcn/ui 组件库

**可以直接 Fork，在它的基础上添加付费功能和列表视图。**

---

## 五、技术栈（推荐）

| 层 | 技术 | 原因 |
|------|------|------|
| 前端框架 | **Next.js 16**（App Router） | prompthub 用的就是这套。服务端渲染对 SEO 友好 |
| UI | **shadcn/ui + Tailwind CSS 4** | 组件美观，无需设计师 |
| 数据库 | **Supabase**（PostgreSQL） | 已有项目就在用，免费额度够你跑一年 |
| 认证 | **Supabase Auth** | 自带邮箱注册 + Google OAuth |
| 支付 | **暂用个人微信支付宝收款码** → ICP 备案通过后接入支付宝 SDK | 验证阶段先不接支付接口 |
| 部署 | **阿里云 ECS**（已买，99 元/年） | 用于 ICP 备案 |
| 语言 | **TypeScript** | prompthub 已经是 TS，可以直接在上面开发 |

---

## 六、MVP 开发目标（两阶段）

### 阶段 1：个人提示词画廊 + 升杯付费（2-4 周）

**1. Fork prompthub 并部署**
- Fork `saifmohamedsv/prompthub` 到你的 GitHub
- 连到你已有的 Supabase 数据库（或新建一个）
- 部署到 Vercel 先测试

**2. 导入现有的 97 条八百库提示词**
- 每一条作为数据库的一条记录
- 上传对应的 ComfyUI 效果图到 Supabase Storage
- 在 prompt 详情页显示效果图 + 提示词文字 + 复制按钮

**3. 粘贴板功能优化（核心改造）**
- **列表/卡片视图切换**：页面顶部一个切换按钮。卡片模式 = 现在的样子（大图 + 标签）；列表模式 = 每一行一条，左侧小缩略图（100×100），中间标题+标签+一行描述，右侧复制按钮
- 列表视图用 URL 参数记住用户偏好（`?view=list` 或 `?view=card`）
- 支持按分类（空间类型、光线、风格、材质）筛选

**4. 升杯付费（MVP 支付）**
- 免费浏览 5 条提示词 → 第 6 条弹窗"解锁全部 300+ 条"→ 展示个人微信收款码 + ¥39 字样
- 收到款后**手动**在 Supabase 数据库里把该用户的 `subscription_tier` 从 'free' 改成 'zhongbei'
- 后续 ICP 备案通过后，替换为支付宝自动支付

**5. 部署到阿里云 ECS**
- ICP 备案提交（用上海首予的材料）
- 部署 Next.js 应用到 ECS
- 域名 meldtip.com 指向 ECS

---

### 阶段 2：社区商城（阶段 1 完成后 2-4 周）

**1. 创作者上传系统**
- 注册用户可以在"创作者中心"上传自己的提示词
- 表单：标题 + 描述 + 提示词正文 + 1-5 张效果图 + 分类 + 标价
- 上传后自动进入"审核中"状态，你在后台审核通过后上线

**2. 佣金系统**
- 每笔交易平台抽 20%
- 创作者在"我的收入"页面看到销售记录和待提现金额

**3. 社区功能**
- 点赞/收藏（prompthub 已有）
- 评论
- 创作者主页 + 排行

---

## 七、粘贴板 · 列表视图的交互细节

> 类比：淘宝/京东搜索结果页。每一行一条商品。

```
┌──────────────────────────────────────────────────────────┐
│ [📷 卡片] [☰ 列表]  ← 视图切换按钮                       │
├──────────────────────────────────────────────────────────┤
│ ┌────────┬──────────────────────────────────┬──────────┐ │
│ │ 效果图  │ 🏠 奶油风衣柜 · 午后暖光           │  [📋 复制] │ │
│ │ 预览    │ #衣柜 #奶油风 #八百库 #ComfyUI    │          │ │
│ │ 100px  │ 28 次复制 · 3 种角度变体          │          │ │
│ └────────┴──────────────────────────────────┴──────────┘ │
│ ┌────────┬──────────────────────────────────┬──────────┐ │
│ │ 效果图  │ 🛏️ 新中式主卧 · 晨光左射            │  [📋 复制] │ │
│ │ 预览    │ #主卧 #新中式 #暖木色            │          │ │
│ │ 100px  │ 15 次复制                         │          │ │
│ └────────┴──────────────────────────────────┴──────────┘ │
└──────────────────────────────────────────────────────────┘
```

---

## 八、项目文件结构（预期）

```
meldtip/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── page.tsx            # 首页（热门提示词）
│   │   ├── browse/             # 浏览页（列表/卡片切换）
│   │   ├── prompt/[id]/        # 提示词详情页
│   │   ├── dashboard/          # 用户后台
│   │   └── creator/            # 创作者中心（阶段2）
│   ├── components/
│   │   ├── PromptCard.tsx      # 卡片视图组件
│   │   ├── PromptRow.tsx       # 列表视图组件
│   │   └── ViewToggle.tsx      # 视图切换按钮
│   ├── lib/
│   │   └── supabase/           # Supabase 客户端
│   └── types/                  # TypeScript 类型
├── supabase/
│   └── schema.sql              # 数据库表结构
└── public/                     # 静态资源
```

---

## 九、竞品参考

| 网站 | 值得参考的 | 不适合的 |
|------|----------|---------|
| **promptbase.com** | 提示词卡片布局、定价展示（$2.99-$6.99）、分类筛选 | C2C 市场太重，需要 Stripe Connect 分账 |
| **gpt-image2.canghe.ai** | 画廊浏览体验、分类+样式+场景三维筛选、Agent Skill 集成 | 他自己的网站不是社区，是官方精选 |
| **opennana.com** | 瀑布流无限滚动、模型/标签筛选 | 内容量大但深度差，不靠付费变现 |
| **promptmart.cn** | 国内部署、社区活跃、浏览量高 | 主要做 AI 视频提示词，不是家居垂直 |
| **prompthub.xin** | 国内对标 prompthub | 信息不够 |
